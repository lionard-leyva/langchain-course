[project]
name = "langchain-course"
version = "0.1.0"
description = "LangChain course - Learning to work with Ollama local models"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-core>=0.3.78",
    "langchain-ollama>=0.3.10",
    "python-dotenv>=1.1.1",
]
